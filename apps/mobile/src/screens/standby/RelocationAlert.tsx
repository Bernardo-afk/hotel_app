import { View, Text } from 'react-native';

export default function RelocationAlert() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>RelocationAlert</Text>
    </View>
  );
}
